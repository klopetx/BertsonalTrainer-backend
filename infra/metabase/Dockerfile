FROM metabase/metabase:v0.50.20

# Intentionally minimal.
# We keep a Dockerfile (instead of using the upstream image directly) so the
# Metabase version is pinned and future extensions (plugins/drivers) can be
# added without touching the rest of the repo.
