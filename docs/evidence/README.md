# Evidence

This folder stores small, reviewable acceptance-evidence summaries (Markdown/JSON).

Raw logs and large artifacts should be written under `evidence/runs/` and are intentionally gitignored.

Authoritative targets and what to record: `docs/acceptance-criteria.md`.
